import * as React from 'react';
import { PrototypeProps, prototypeConnect } from './scope';


function Timeline(props: PrototypeProps) {
    return (
        <React.Fragment>
        </React.Fragment>
    );
}


export default prototypeConnect()(Timeline);
